#!/bin/sh
# Advisory only: editing and commits are never controlled by Verify.  Run the
# two explicit end-of-work checks when the change is ready for self-review.
printf '%s\n' 'LexiFlow reminder: run scripts/gates/change_verify.py and scripts/gates/repository_verify.py before handoff.' >&2
exit 0
