# 1. 派发预检：验收

## 1.1. 必测范围

测试覆盖合法无冲突输入、任务/版本/哈希不一致、owner 缺失或跨越、允许范围与声明不一致、路径语法/符号链接/大小写风险，以及写入和公开合同冲突。输入使用合成任务和路径，不使用个人内容或运行数据。

## 1.2. 证明边界

测试证明判定器对冻结输入的三态语义；不证明活动实例真实完整、跨进程原子派发、Qoder 生命周期或 Gate 验收。`PASS` 后仍由调用方在有效窗口内完成派发并遵守共享 policy。
