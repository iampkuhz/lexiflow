# 1. 工具链复现：干净环境

## 1.1. 执行

在不复用开发机产物的环境中，从仓库入口构建：

```bash
python3 -m scripts.toolchain.java_gradle clean deliveryFull
```

记录环境、命令、实际输出和构建产物。若网络、运行时或依赖条件不满足，按实际原因记 `BLOCKED` 或 `FAIL`；不能用已有缓存或旧报告代替本次重建。

## 1.2. 边界

该检查证明指定环境的工具链重建，不证明产品业务旅程、性能或正式 Gate 验收。
