Read and follow the repository-root `AGENTS.md`. Before every Codex Sub-Agent dispatch, resolve and apply `subagent_protocol.codex_model_policy` in `harness/agent-policy.manifest.yaml`, including Luna-first default selection, evidence-based escalation, whole-package reasoning classification and explicit model/reasoning arguments. The shared policy, not a role name, inherited session model or prompt-only model request, selects the dispatch model. Read current shared rules rather than retaining an older cached model policy. This file is an entry point only; ownership, validation, callback, concurrency and Qoder rules remain in AGENTS.md and Harness.
